# haproxy-openshift-dynamic
Creates a dynamic haproxy configuration based on your Openshift cluster infrastructure

This should be run with privileged user in the same machines where HAProxy service runs. 
